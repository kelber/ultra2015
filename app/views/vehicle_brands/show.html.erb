<h1><%= image_tag("logoultra250CinzaEscuro.png", size: "100x100", alt: "Ultravisao") %>  Veiculos | <%= @vehicle_brand.name %></h1>
<%= link_to 'Voltar', vehicle_brands_path, class: "btn btn-default" %>
																																										<!-- , @vehicle_name) -->
<%= link_to 'Cadastrar Veiculo', new_vehicle_brand_vehicle_name_path(@vehicle_brand)  , class: "btn btn-primary" %>
<hr>


	<h1></h1>

	<% @vehicle_brand.vehicle_names.each do |vehicle| %>
		<h3><%= link_to vehicle.name , vehicle_brand_vehicle_name_path(@vehicle_brand, vehicle) %></h3>
		<%= link_to 'Deletar',  vehicle_brand_vehicle_name_path(@vehicle_brand, vehicle), method: :delete, data: { confirm: "Voce tem certeza?" } ,class: "btn btn-danger" %>

	<% end %>
